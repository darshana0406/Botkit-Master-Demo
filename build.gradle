apply plugin: 'java'

repositories {
    jcenter()
}

dependencies {
    implementation 'org.json:json:20210307'
}

task runJava(type: JavaExec) {
    main = 'Export' // Replace 'HelloWorld' with the name of your main class if different
    classpath = sourceSets.main.runtimeClasspath
}